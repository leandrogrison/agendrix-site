import { TimeInlinePipe } from './time-inline-pipe';

describe('TimeInlinePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeInlinePipe();
    expect(pipe).toBeTruthy();
  });
});
